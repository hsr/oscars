Change the publishing point in config/coord.yaml as needed

To build, run:

mvn package


To start the server, run:
java -jar target/trivCoord-0.0.1-SNAPSHOT.one-jar.jar

from this directory.

This trivial implementation pretty much returns null.